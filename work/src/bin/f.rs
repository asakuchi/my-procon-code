use proconio::input;
// use proconio::fastout;

// #[fastout]
fn main() {
    input! {
        n: usize,
        a: [usize; n],
        // m: usize,
        // u_v: [(Usize1, Usize1); n],
        // p: [[usize; w]; h],
        // s: String,
    }
}
